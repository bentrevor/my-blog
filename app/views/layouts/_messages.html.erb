<% flash.each do |name, msg| %>
  <% if msg.is_a? String %>
    <div class="span8 offset2 alert alert-<%= name %>">
      <a class="close" data-dismiss="alert">&#215;</a>
      <%= content_tag :div, msg, id: "flash_#{name}" %>
    </div>
  <% end %>
<% end %>